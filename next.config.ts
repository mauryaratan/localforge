import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Empty turbopack config to allow Next.js 16+ to run with Turbopack
  turbopack: {},
};

export default nextConfig;
